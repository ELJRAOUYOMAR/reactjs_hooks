// this file is just a helper 
// we need the Usercontext in more FileSystem, so we declare what we need in this helper file 

import {createContext} from 'react'

const UserContext = createContext(null);

export default UserContext;
